# Настройки tnt-mutants для собственного каталога утилиты.
#
# Образцы самопроверки в обычный прогон не входят: у них есть выжившие
# по замыслу, и прогон по изменённым файлам валился бы на них.
MUTANTS_SOURCES=(
    ':(glob)**/*.lua'
    ':(exclude,glob)selftest/**'
)
